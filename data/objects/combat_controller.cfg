{
	id: "combat_controller",
	is_human: true,
	hitpoints: 4,
	editor_info: { category: "player" },
	animation: [{
		id: "normal",
		image: "cube.png",
		rect: [0,0,15,15],
		collide: [0,0,15,15],
		frames: 1,
		duration: 1000,
	}],
	
	is_strict: true,
	
	//on_key_press_a: "[if(level.camera, set(level.camera.position, zip(level.camera.position, map(level.camera.right, value * 10.0 * level.camera.speed), a-b)))]",
	//on_key_press_d: "[if(level.camera, set(level.camera.position, zip(level.camera.position, map(level.camera.right, value * 10.0 * level.camera.speed), a+b)))]",
	//on_key_press_s: "[if(level.camera, set(level.camera.position, zip(level.camera.position, map(level.camera.direction, value * 10.0 * level.camera.speed), a-b)))]",
	//on_key_press_w: "[if(level.camera, set(level.camera.position, zip(level.camera.position, map(level.camera.direction, value * 10.0 * level.camera.speed), a+b)))]",
	//on_process: "map(['w','a','s','d'], if(value in level.player.ctrl_keys, fire_event('key_press_' + value)))",
	
	on_create: "[
		// console_output_to_screen(false), 
		fire_event('window_resize', {width: level.camera_position[2], height: level.camera_position[3]}),
		if(grounds and grounds.flat_plane, set(level.isomap, grounds.flat_plane)),
		//set(level.mouselook, true)
	] where grounds = get_document('data/battlegrounds.cfg')",
	
	on_window_resize: "[
		set(level.dimensions, [0, 0, width, height]),
	] where width = int<- arg.width, height = int<- arg.height",
}